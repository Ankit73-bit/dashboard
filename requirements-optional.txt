# Optional heavy packages (AI background removal for Paras Print tools).
# Installed by setup.bat after core requirements.
# Failures here do NOT block the dashboard from running.

rembg>=2.0.50,<3
onnxruntime>=1.16.0,<2
